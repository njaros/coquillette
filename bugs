- echo $? >b | echo $? >>b => last_return est le meme,
le deuxieme echo n'attend pas que last return soit mis a jour
- tabs ?
- ctrl -D avec cat
- bug d'affichage quand commandes trop longues, pas de saut de ligne
- 