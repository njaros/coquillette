- echo $? >b | echo $? >>b => last_return est le meme,
le deuxieme echo n'attend pas que last return soit mis a jour
- tabs ?
- bug d'affichage quand commandes trop longues, pas de saut de ligne
- tester fds -1

## EXIT ##

exit -- 42 attention pas too many arguments, checker si le premier argue contient des chiffres ?

exit "          " bash: exit:           : numeric argument required 255

blop
blop: command not found
echo $? > blop
127 alors qu'on a 0