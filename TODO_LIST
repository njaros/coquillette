## tri des suggestions de la plus importante a la moins important ##

C - recuperer le "WEXITSTATUS" du wstatus du wait - vérifier avec ctrl c 

C - builtin EXIT : revoir les cas d'erreur !

C - vérifier sur bash ce qu'il se passe quand on a un fd = -1, ajouter cette
condition dans les builtins (echo, env, etc), et si je close un fd = -1, que
se passe-t-il ? -  à vérifier

- echo $? >b | echo $? >>b 127 et 0 => piper les builtins

- bonus ? ou la flemme ?

- termcaps ?

### MODIFS LIBFT ###

- securiser le ft_lstdelone

- discuter des fonctions qui seront les plus usuelles pour les integrer

### QUESTIONS ###

- bash -c 