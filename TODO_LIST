## tri des suggestions de la plus importante a la moins important ##

- faire en sorte que les historiques ne fassent pas de bug d'affichage

- bug avec une seule lettre dans le prompt à régler

C - saut à la ligne avec les builtins ?! à vérifier

C - recuperer le "WEXITSTATUS" du wstatus du wait - en cours

C - builtin EXIT : revoir les cas d'erreur ! ajouter une sous-fonction qui free data, etc

C - builtin PWD : si getcwd ne fonctionne pas (par exemple, avec le cas où je me trouve dans un dossier qui a été supprimé), je dois quand même pouvoir afficher pwd => récupérer la variable dans ma liste chainée ?? à tester !!

C - finir de sécuriser malloc, etc dans les builtins

C - norminer builtins !!!!

C - essayer d'utiliser stat dans check path pour vérifier si c'est une commande ou dossier ?
C - si PATH est unset, quel message afficher, à vérifier...
C - revoir la fonction check_path...

C - réorganiser fonctions et fichiers

C - vérifier sur bash ce qu'il se passe quand on a un fd = -1, ajouter cette condition dans les builtins (echo, env, etc), et si je close un fd = -1, que se passe-t-il ?

- modifier le SHLVL ? quand on lance minishell depuis notre minishell

- bonus ? ou la flemme ?

- termcaps ?

- fd permission denied ?

### MODIFS LIBFT ###

- securiser le ft_lstdelone

- discuter des fonctions qui seront les plus usuelles pour les integrer

### QUESTIONS ###

C - modifier variable d'environnement _ (avec la dernière commande appelée ?) => demander aux autres groupes

C - besoin de vérifier les dup2, open, close ?

- bash -c 